<div class="row justify-content-md-center mt-5 mb-5">
    {{ form_start(form, {'attr': {'class': 'col-6 border border-info rounded p-3 text-center'}}) }}
    <p class="h4">{{ title }}</p>
    <div class="form-group">
        <label for="label_name">Nom du groupe</label>
        {{ form_widget(form.name, {'attr': {'class': 'form-control border-info'}}) }}
    </div>
    <div class="form-group">
        <label for="label_description">Liste des utilisateurs</label>
        {{ form_widget(form.users, {'attr': {'class': 'form-control border-info'}}) }}
    </div>
    <button type="submit" class="btn btn-info">{{ button_label }}</button>
    {{ form_end(form) }}
</div>
